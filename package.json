{
  "name": "backend",
  "version": "1.0.0",
  "description": "Backend for the MABS project",
  "main": "server.js",
  "scripts": {
    "start": "NODE_ENV=production node server.js",
    "dev": "nodemon server.js",
    "build": "echo 'No build step for backend'"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.0.0",
    "express": "^4.18.2",
    "mongoose": "^7.0.0",
    "nodemailer": "^7.0.2"
  },
  "devDependencies": {
    "nodemon": "^2.0.22"
  }
}
